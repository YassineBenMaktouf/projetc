#include<stdio.h>
#include "yassine_function.h"

//add ETS
void addETS(struct ETS E)
{
FILE* f;
char nomETS[50],region[20],id[20],director[50],creat_date[10];
int cap_pat,phone,cap_doc;
f=fopen("home/yassine/Desktop/listeETS.txt","a+");
	if(f!=NULL)
	{
		
		fclose(f);
	}
}
//delete ETS
void delETS(struct ETS E)
{
}
//edit ETS
void editETS(struct ETS E)
{
}
//----
void rech_reg(struct ETS E)
{
}

void rech_cap(struct ETS E)
{
}

void map(struct ETS E)
{
}

